DB_HOST=localhost
DB_PORT=5432
DB_USER=team_mex
DB_PASSWORD=team_mex
DB_NAME=team_mex_mtto
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Andon outbound. Default noop/log. Twilio aparcado. Evolution: otro agente (no este PR).
# ANDON_NOTIFY_PROVIDER=noop
# ANDON_NOTIFY_PROVIDER=twilio  # parked; requiere SID/token/from/ANDON_OPS_PHONES
# Evolution API no se cablea aquí (ToS / no prod).
