# Shaping Workspace

Use for comparing solution options before commitment.

Typical contents:
- options;
- trade-off analysis;
- proposal;
- constraints;
- decision record before formal SPEC/ADR.

Keep `PROPOSAL` and `DECISION` distinct.
Once accepted, move durable behavior into SPEC/domain/ADR artifacts.
