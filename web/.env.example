# El navegador llama a /backend (mismo origen). Next reescribe a API_URL.
NEXT_PUBLIC_API_BASE=/backend
# Local: API en :3001. Vercel: URL pública de Railway, sin barra final.
API_URL=http://localhost:3001
