# UI Team Mex — Mantenimiento

Cliente delgado Next.js (App Router). El README del repositorio describe el flujo Slice 1.

```bash
npm install
npm run dev   # http://localhost:3000  (proxy /backend → API :3001)
```
